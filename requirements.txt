# CuMIN — core runtime dependencies
#
# Verified end-to-end on this exact set: full `--mode all` run on a big-endian
# 600-frame stack with a 4-ROI RoiSet completed with 0 errors, all modules
# import clean, no DeprecationWarnings raised.
#
#   pip install -r requirements.txt
#
# Optional GPU / motion-correction extras: requirements-optional.txt
# Notebook (JupyterLab) extras:            requirements-notebook.txt

# --- Numerics & data ---
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
xarray==2026.7.0
networkx==3.6.1
POT==0.9.7

# --- Imaging & I/O ---
scikit-image==0.26.0
tifffile==2026.7.14     # <2024.2.12 calls ndarray.newbyteorder(), removed in numpy 2.0
opencv-python==5.0.0.93
h5py==3.16.0            # <3.12 was built against the numpy 1 C API
MedPy==0.5.2

# --- ROI handling (ImageJ/FIJI RoiSet .zip) ---
read_roi==1.6.0
roifile==2026.2.10

# --- Analysis ---
scikit-learn==1.9.0

# --- Plotting ---
matplotlib==3.11.1
seaborn==0.13.2

# --- Excel summary export (imported by modules/utils.py) ---
openpyxl==3.1.5
XlsxWriter==3.2.9

# --- Config, CLI, logging ---
PyYAML==6.0.3
tqdm==4.69.0
psutil==7.2.2
